{
  "name": "muba-push",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "pm2 start app.yaml",
    "stop": "pm2 stop app.yaml && pm2 delete app.yaml"
  },
  "dependencies": {
    "dotenv": "^6.1.0",
    "http": "0.0.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2",
    "websocket": "^1.0.28"
  }
}
